namespace Bloodonor.Services
{
    public class StudentService /*: IStudentService*/
    {
        //private readonly IMongoCollection<Student> _students;
        //private readonly ApplicationContext _context;

        //public StudentService(IStudentStoreDbSettings settings,/* IMongoClient mongoClient*/)
        //{
        //    var database = mongoClient.GetDatabase(settings.DatabaseName);
        //    _students = database.GetCollection<Student>(settings.studentCoursesCollectionName);
        //}
        //public Student Create(Student student)
        //{
        //    _students.InsertOne(student);
        //    return student;
        //}

        //public List<Student> Get()
        //{
        //    return _students.Find(student => true).ToList();
        //}

        //public Student Get(string id)
        //{
        //    return _students.Find(student => student.Id.Equals(id)).FirstOrDefault();
        //}

        //public void Remove(string id)
        //{
        //    _students.DeleteOne(student => student.Id.Equals(id));
        //}

        //public void Update(string id, Student student)
        //{
        //    _students.ReplaceOne(student => student.Id.Equals(id), student);
        //}
    }
}